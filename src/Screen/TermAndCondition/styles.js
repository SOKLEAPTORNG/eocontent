import {StyleSheet} from 'react-native';
import {Color} from '../../Constant';

export const styles = StyleSheet.create({
  titleLabel: {
    fontSize: 18,
    color: Color.LIGHT,
    marginVertical: 10,
  },
  descriptionText: {
    fontSize: 14,
    color: Color.LIGHT,
    textAlign: 'justify',
  },
});
